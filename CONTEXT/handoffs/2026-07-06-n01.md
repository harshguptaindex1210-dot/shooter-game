# Handoff: Browser Battle Royale MVP — N-01 Complete

**Date**: 2026-07-06
**Phase**: /part2 (implementation) → ready for N-02
**Branch**: main
**Code commit**: 611b5a7ffd6203ee486ad92107682425908a868c

## What Was Done

Implemented N-01: project scaffold, build pipeline, CI.

- Vite + TypeScript + Three.js project.
- `index.html`, `src/main.ts` (black canvas render loop), `src/renderer.ts`.
- Vitest harness with one test: `createRenderer` configures renderer from canvas.
- ESLint (typescript-eslint) + Prettier configured.
- GitHub Actions `.github/workflows/ci.yml`: install, typecheck, lint, test, build.
- `scripts/bundle-size.js` enforced during build; initial bundle ~114 KB gz (limit 500 KB).
- `lighthouserc.json` stub for Lighthouse CI (asserts FCP ≤2s, TTI ≤8s, total bytes ≤512KB).

## Gate Command

`npm run build && npm run lint && npm run typecheck && npm test`

Result: **exit 0**. Build output 114 KB gz, under the 500 KB INV-3 cap.

## Red-Team Pass

Deferred native `WebGLRenderer` in unit tests because jsdom has no WebGL context. Test mocks `three.WebGLRenderer` to verify `createRenderer` passes correct config (canvas, antialias, size, pixel ratio clamp). Trade-off: real GL behavior is covered later by N-02's headless Puppeteer benchmark.

No destructive paths, no secrets, no auth in this issue.

## Unfixed Edge Cases / Follow-ups

- Lighthouse CI not wired into GitHub Actions yet; needs `@lhci/cli` install + action step.
- Render benchmark script `bench:render` is a placeholder in `package.json`; implemented in N-02.
- `vite.config.ts` manualChunks splits three into its own chunk; revisit after asset streaming design in N-13.

## Next Issue to Start

**N-02: Three.js render core + camera + blockout scene**

- `Scene`, `PerspectiveCamera`, `WebGLRenderer` setup.
- 1x1 km ground plane + 4 POI box clusters + instanced props.
- Third-person orbit camera.
- Quality presets.
- Headless render benchmark via Puppeteer.
- `Verification-command`: `npm run bench:render`

## Risks to Watch

1. **Rollback at scale** (Critical) — still mitigated by N-06 gate.
2. **Browser memory ceiling** (Medium) — profile at N-13.
3. **Three.js WebGL context in tests** — N-02's Puppeteer benchmark will validate real renderer; jsdom unit tests continue to mock GL.

## Push Status

Pushed to `origin/main` after this handoff is committed.

Run `/part2` to start implementing N-02.
